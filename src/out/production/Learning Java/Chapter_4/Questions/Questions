1:
    Класс отличается от объекта тем, что класс это всего лишь описание типа данных и кода,
     который этими данными управляет. Объект же содержит собственные копии всех методов и переменных экземпляра,
     определенных в классе.

2:
    Класс определяется ключевым словом class название_класса {}
    Объявление класса создает новый тип данных. В данном случает этот тип
    называется название_класса.

3:
    Объект содержит собственную копию экземпляра класса.
    (В этой копии переменные, методы)

4:
    Объявление объекта в две отдельные инструкции:
        MyCounter counter;
        counter = new MyCounter();

5:
    double myMeth(int a, int b) {}

6:
    Метод который возвращает некоторое значение, должен завершать свое выполнение конструкцией return значение;
     где значение того же типа данных, что и при объявлении метода.

7:
    Имя конструктора должно совпадать с именем класса.

8:
    Оператор new возвращает ссылку на вновь созданный объект, который получает
     переменная класса в результате присваивания в данной форме записи:
     переменная_класса = new имя_класса(список_аргументов)

9:
    Сборка мусора освобождает память от неиспользуемых объектов,
     чтобы сделать ее доступной для последующего перераспределения.

10:
    Ключевое слово this обозначает именно тот объект, по ссылке на который действует вызываемый метод.

11:
    Да, конструктор может иметь один или несколько параметров.

12:
    Если метод не возвращает значения, то его тип объявляется: void