1:
    Суперкласс не имеет доступа к членам подкласса.
    Но вот подкласс имеет доступ к членам суперкласса.

2:
    CircleDemo.java

3:
    Для того что бы предотвратить обращение к членам суперкласса из подкласса,
    их необходимо объявить закрытыми (private)

4:
    Ключевое слово super:
    Используется для вызова конструктора суперкласса из подкласса.
    Используется для доступа к членам суперкласса.

5:
    Конструкторы будут вызываться в следующем порядке:
    1.Alpha (суперкласс)
    2.Beta
    3.Gamma

6:
    Переменная ссылки на суперкласс может указывать на объект подкласса,
    это важно, т.к. это определяет доступ к конкретным членам класса.
    Т.е. если ссылка на объект подкласса присваивается ссылочной переменной суперкласса,
    то последняя может быть использована для доступа только к тем частям данного объекта,
    которые определяются суперклассом.
     В Java этот принцип используется для вызова переопределяемых методов во время выполнения.
    Если вызов переопределенного метода осуществляется с использованием ссылки на суперкласс,
    то исполняющая среда Java выбирает нужную версию метода на основании типа объекта,
    на который эта ссылка указывает в момент вызова.

7:
    Абстрактный класс - в таком классе определяется лишь суть методов,
    которые должны быть конкретно реализованы в подклассах, а не в самом суперклассе.
    (Т.е. эти абстрактные методы должны быть переопределены в подклассах)

8:
    Для предотвращения переопределения метода и наследования класса
    - указывается ключевое слово final: final class ...

9:
    Наследование, переопределение методов и абстрактные классы,
    используются для поддержки полиморфизма - один интерфейс,
    множество методов. Т.е. создается абстракный класс с методами
    'заготовками', далее они переопределяются в подклассах.
     Здесь интерфейс это абстрактный класс, а множество методов -
    переопределенные методы в подклассах.

10:
    Класс Object является суперклассом для всех остальных классов.

11:
    Верно, класс который содержить хотя бы один абстрактный метод,
    должен быть объявлен абстрактным.

12:
    Для именованной константы следует использовать ключевое слово final.